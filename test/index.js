"use strict";

const assert = require("assert");

describe("WIP Unit Test", function() {
  it("should be written soon! This is just a test.", function() {
    assert(true);
  });
  it("will also have coveralls reports! Don't mind the coverage errors.", function() {
    assert(true);
  });
});
